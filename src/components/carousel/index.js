import React from "react";

import "./carousel.css"; // Import CSS Modules

const Carousel = () => {
  return (
    <div className={"container"}>
      <div className={"slideContent"}></div>
    </div>
  );
};

export default Carousel;
